{
  "name": "infra",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "bin": {
    "infra": "bin/infra.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "format-check": "rome format function bin lib",
    "format": "rome format --write function bin lib",
    "check": "rome check function bin lib",
    "check-fix": "rome check --apply-unsafe function bin lib",
    "prelint": "yarn install",
    "prebuild": "yarn install",
    "prefix": "yarn install",
    "precheck": "yarn install",
    "precheck-fix": "yarn install",
    "preformat": "yarn install",
    "preformat-check": "yarn install",
    "lint": "yarn format-check && yarn check",
    "lint-fix": "yarn format && yarn check-fix"
  },
  "devDependencies": {
    "@swc/core": "*",
    "@swc/helpers": "*",
    "@types/node": "*",
    "rome": "*",
    "regenerator-runtime": "*",
    "ts-node": "*",
    "typescript": "*"
  },
  "dependencies": {
    "aws-cdk-lib": "*",
    "constructs": "*",
    "source-map-support": "*"
  }
}
